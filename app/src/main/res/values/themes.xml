<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TPSeminarioDeLenguajes" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores Modo Dia -->
        <item name="background">@drawable/background_gradient</item>
        <item name="android:toolbarStyle">@color/toolbarColorDay</item>
        <item name="android:textColor">@color/whiteDay</item>
        <item name="colorPrimary">@color/primaryLight</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="android:textColorHint">@color/hintDay</item>
        <item name="colorControlNormal">@color/checkboxTintDay</item>
    </style>

    <style name="Theme.TPSeminarioDeLenguajes" parent="Base.Theme.TPSeminarioDeLenguajes" />
</resources>